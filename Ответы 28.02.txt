4-Статические объекты одной и той же единицы трансляции инициализируются в том же порядке, в котором они определены, а Статические объекты разных единиц трансляции инициализируются в неопределённом порядке.
6-значение, ссылка, функция, тип, нестатический член класса, шаблон, пространство имён, this
21-Возврат по значению, возврат по ссылке, возврат структуры/кортежа, возврат через исключения, возврат через глобальные переменные
26-&-Ссылка, &&-И
40-Конструктор — это специальный метод класса, который автоматически вызывается при создании объекта, начальное состояние обЪекта. 
75-Будет переопределение f()